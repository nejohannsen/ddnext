%h1 All Races
=link_to "Create New Race", new_race_path()
- unless @races.empty?
  %table
    %tr
      %th Race
      %th Actions
    - @races.each do |race|
      %tr
        %td= race.title
        %td= " #{link_to 'View', race} | #{link_to 'Edit', edit_race_path(race)}".html_safe
